export default {
  /**
   * A  =   A0 + A1 * 0.1
   * B  =   B0 + B1 * 0.1
   * C  =   C0 * 10 + C1 + C2 * 0.1 + C3 * 0.01 = A * B
   *    =   (A0 + A1 * 0.1) * (B0 + B1 * 0.1)
   *    =   A0 * B0 + A0 * B1 * 0.1 + A1 * 0.1 * B0 + A1 * 0.1 * B1 * 0.1
   *    ==> 0 <= Ai * Bi < 100
   *    ==> 0 <= Ai * Bi * 0.1 < 10
   *    ==> 0 <= Ai * Bi * 0.1 * 2 < 20
   *    ==> 0 <= Ai * Bi * 0.1 * 0.1 < 1
   */
  test() {
  }
};
